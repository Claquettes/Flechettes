if((Math.random() < 0.95)&&(Math.random() > 0.475)){ //47.5% de chance
                            triggerNormal();
                        }
                        if(Math.random() >= 0.95){ //5% de chance
                            triggerGoldenCarrot();
                        }
                        else{
                            triggerNormal();
                        }